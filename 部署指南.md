# 第五屆善終關懷研討會 · 電子展刊 — GitHub Pages 部署指南

## 這個文件夾裡有什麼

```
github-pages/
├── index.html          ← 展刊主頁（必須叫這個名字）
├── qr-checkin.jpg      ← 會議簽到二維碼
├── speakers/           ← 五位講者頭像
│   ├── spk1.jpg ~ spk5.jpg
└── .nojekyll           ← 必需的空文件，告訴 GitHub 不要處理它
```

**總共只有 160KB**，加載很快。全部文件必須一起上傳，目錄結構不能變。

---

## 第一步：註冊 GitHub 帳號

1. 打開 https://github.com/signup
2. 填寫：
   - **Email**：你的郵箱
   - **Password**：至少 8 位，含數字和小寫字母
   - **Username**：英文用戶名（例如 `cityu-symposium`），**這個會出現在你的網址裡，想好再填**
   - 完成人機驗證
3. 去郵箱收驗證碼，填入即可

> ⚠️ **用戶名建議用純英文小寫 + 連字符**，例如 `cityu-eol-care`、`macau-symposium`。
> 你的最終網址會是：`https://用戶名.github.io/倉庫名/`

---

## 第二步：建立倉庫（Repository）

1. 登入後，點右上角 **「+」→ New repository**
2. 填寫：
   - **Repository name**：`exhibition`（或你自己喜歡的名字）
   - **Description**：可留空
   - **Public** ✅ 必須選公開（私有倉庫的 Pages 要付費）
   - **不要**勾選 "Add a README file"
3. 點 **Create repository**

---

## 第三步：上傳文件（用網頁操作，最簡單）

1. 進入剛建立的倉庫頁面
2. 點 **「uploading an existing file」** 連結（或 Add file → Upload files）
3. **把 `github-pages` 文件夾裡的東西全部拖進去**：
   - `index.html`
   - `qr-checkin.jpg`
   - `.nojekyll`
   - `speakers` 文件夾（**整個文件夾拖進去**，GitHub 會自動保留目錄結構）
4. 底部 Commit changes 框隨便寫一句，例如 `add exhibition`
5. 點綠色 **Commit changes**

> ⚠️ **`.nojekyll` 是隱藏文件**，在文件管理器裡可能看不到。
> 如果拖不進去，可以之後手動建立：Add file → Create new file → 文件名填 `.nojekyll` → 內容留空 → Commit。

**上傳後目錄應該是這樣：**
```
index.html
qr-checkin.jpg
.nojekyll
speakers/spk1.jpg
speakers/spk2.jpg
speakers/spk3.jpg
speakers/spk4.jpg
speakers/spk5.jpg
```

---

## 第四步：開啟 GitHub Pages

1. 在倉庫頁面點 **Settings**（齒輪圖標）
2. 左側選單找到 **Pages**
3. **Source** 選 `Deploy from a branch`
4. **Branch** 選 `main`，文件夾選 `/ (root)`
5. 點 **Save**
6. **等 1~2 分鐘**，刷新頁面，頂部會出現綠色提示：

```
Your site is live at https://用戶名.github.io/exhibition/
```

**這就是你的永久網址。**

---

## 第五步：驗證

1. 用**手機瀏覽器**打開那個網址，確認能正常顯示
2. 用**微信掃一掃**掃描文件夾裡的 `qr-exhibition-card.png`（如已生成）
3. 確認：
   - 頁面能打開
   - 五位講者頭像正常顯示
   - 底部簽到二維碼可見
   - 點擊簽到網址能跳轉到萬卷星

---

## 常見問題

**Q：網址太長記不住？**
A：可以用短網址服務（如 dwz.cn）轉短。但**二維碼直接用長網址更可靠**，不需要縮短。

**Q：內容要更新怎麼辦？**
A：直接在 GitHub 網頁上點開對應文件 → 點鉛筆圖標編輯 → Commit。
**網址不會變，二維碼不用重印。**

**Q：打不開 / 404？**
A：
1. 確認 `index.html` 在倉庫**根目錄**（不是在某個子文件夾裡）
2. 確認 Settings → Pages 裡顯示了綠色的 live 提示
3. 剛開通要等 1~2 分鐘
4. 確認倉庫是 **Public**

**Q：微信裡打開顯示「非微信官方網頁」警告？**
A：這是微信的常規提示，點「繼續訪問」即可。GitHub Pages 是正規域名，不會被封。

**Q：圖片顯示不出來？**
A：檢查 `speakers/` 文件夾是否上傳成功，且文件名是小寫 `spk1.jpg`（區分大小寫）。

---

## 部署完成後

把網址告訴我，我用它**重新生成二維碼**，並再次驗證。這個網址是永久的，二維碼印出去後不用擔心失效。
